    /*

        Esercizio:  Scrivere una function C che ha come input un array di struct (struct punto, con campo ascissa e campo ordinata) che individuano punti nel piano, 
                    e il suo size n e che fornisce in output la matrice nxn delle distanze, dove sono memorizzate le distanze di ogni punto da tutti gli altri. 
                    La function deve fare uso di un'altra function, anch'essa da scrivere) che restituisce la distanza tra due qualunque punti del piano

    */


    // Include della libreria 
    #include <stdio.h>
    #include <string.h>

    // Definizione della struttura da usare nel codice, dichiarata in testa al codice per essere usata 
    typedef struct {
        float x;
        float y;
    } Punto_Cartesiano;

    // Recall delle funzioni 


    int main (){

        

        return 0;
    }